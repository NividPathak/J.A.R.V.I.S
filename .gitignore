# Environment & secrets — broad on purpose, this bit went wrong last time
.env
.env.*
*.env
!.env.example
secrets/
*credentials*
*.pem
*.key

# Runtime state (cache db, logs, pids)
var/
*.log
*.pid

# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Model artifacts. The final LoRA adapter is ~14MB and worth committing — it's
# the deliverable. The numbered checkpoints it passed through are not.
models/
*.gguf
checkpoints/
training/adapters/[0-9]*_adapters.safetensors

# macOS
.DS_Store
.AppleDouble

# IDE
.vscode/
.idea/
*.swp
